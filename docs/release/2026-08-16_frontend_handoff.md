# 프런트엔드 작업 인계 — 2026-08-16

대상: `choidev777-bit/dayjaview` `main`
작성 목적: 오늘 `main`에 올린 화면 작업이 무엇이고, 무엇이 실제로 도는지, 무엇이 아직 없는지를 한 장으로 넘기기 위함.

---

## 1. 올린 커밋

| 커밋 | 내용 |
|---|---|
| `0101458` | 과거 사례 화면 3개 신설(유사사례 목록·과거 이벤트 상세·과거 소재 유형 상세) + 시안 대조 반영 |
| `f266e4f` | 화면 전체 실패용 오류 페이지 분리 |
| `dce8759` | 시안 실측 대조로 드러난 화면 문제 수정 |
| `9807512` | 시연 화면을 구 DB 실데이터로 채움 |
| `b7c2cce` | 시연 화면 데이터·배치 오류 수정 |
| `835a104` | 화면 지적 6건 + 뒤로 가기 상태 유지·휠 속도 |

`0101458~1..835a104` 기준 32개 파일, +18,017 / −216.

---

## 2. 화면 작업

### 2.1 새로 만든 화면 3개

`screen_spec`의 과거 영역 화면이 비어 있어 시안 구조에 맞춰 채웠다.

| 화면 | 경로 | 비고 |
|---|---|---|
| 유사사례 목록 | `/themes/:themeId/events/:eventId/similar` | 기간 토글(다음날·5거래일·20거래일), 기간별 분모 각각 표기 |
| 과거 이벤트 상세 | `/events/:eventId` | 당시 결과·당시 주도 종목·오늘과 비슷한 이유 |
| 과거 소재 유형 상세 | `/catalysts/:catalystId` | 과거 동반 기록·기간별 중앙 반응·집계에 포함된 사건 |

지킨 규칙:
- 평균이 아니라 **중앙값**, 기간마다 **분모를 따로** 적는다 (`screen_spec` 8.8)
- 확률 표현을 쓰지 않는다 (`screen_spec` 13.2)
- 표본이 적으면 건수 바로 아래에 참고용임을 적는다
- 미래 결과를 사례 선택에 쓰지 않았음을 화면에 남긴다

### 2.2 오늘 고친 것 (지적사항 반영)

| 지적 | 원인 | 조치 |
|---|---|---|
| 저장 목록에 저장한 적 없는 항목이 계속 보임 | 계약 fixture의 저장 목록이 초기값으로 들어감 | 시연 모드는 빈 목록에서 시작 |
| 테마를 바꿔도 숫자가 그대로 | 관심 공백이 fixture 고정값 | 테마별 실제 거래일 수. 1거래일이면 배지 자체를 빼다 |
| `데이터 반영` 칸이 어그러짐 | 설명이 혼자 3줄로 접힘 | `핵심 14/14종목` 한 줄로 |
| 사례가 너무 많이 보임 | 전량 노출 | 3건 + 아코디언 |
| `같은 테마의 과거 기록` 태그가 당연한 말 | 고정 문구 | `keyword.occurrence_keyword_links`의 실제 키워드. 없으면 태그 줄을 안 그림 |
| 오늘 현황 근거가 다른 테마 뉴스 | `main.fixture.tsx`가 evidence·treemap·saved에 항상 fixture 기본값을 넣어 시연 분기가 죽어 있었음 | 파라미터를 준 경우에만 fixture |
| 급부상 배지 | **계산이 아니라 1위 고정이었음** | 제거. 실제 규칙은 `packages/pipeline/market.py:919` |
| 실시간 화면에 테마 1개만 | 트리맵이 fixture 1건 | 그날 테마 10건 전부, 배치는 2/3/3/2 |
| 실시간 제목 아래 애매한 구분선 | `.page-intro` 하단 선이 데이터 상태 박스와 겹침 | 제거 |
| 계산 기준이 두 줄뿐 | `screen_spec` 3.4 미충족 | 여섯 항목(질문·대상과 기간·가중 방식·기준 시각·제외와 결측·예측 아님)으로 확장 |
| 접기 버튼이 왼쪽 | 좌우 여백이 `margin: auto`를 덮음 | 가운데 |
| 칸마다 높이가 다름 | `중앙 +22.6%`가 칸에 따라 두 줄로 접힘 | `중앙`을 윗줄로 분리, 값은 줄바꿈 안 함 |
| `당시 결과` 박스가 빨강 고정 | 배경색 고정 | 상승 분홍 / 하락 파랑 / 관찰 중·기록 없음 회색 |
| 소재 상세 → 사건 클릭 시 다른 테마 사례로 이동 | 그 사건이 시연 데이터에 없어 fixture로 빠짐 | 소재 사건도 실데이터로 연결 |

### 2.3 팀원 요청 반영

1. **휠 속도** — 끌기 0.55배, 휠 0.45배로 늦추고, 손을 뗀 뒤 가장 가까운 카드로 맞춘다.
2. **뒤로 가기 상태 유지** — 휠 위치·상세 탭·스크롤 위치를 되살리고 첫 진입 회전 연출은 건너뛴다 (`ui_prototype_adaptation_plan` 5.1). 세션 메모리에만 두며 서버로 보내지 않는다.
3. 저장 탭 다중 삭제는 넣지 않았다. 이미 항목마다 저장 해제가 있고, 다중 선택은 `screen_spec`에 없는 조작이다.

### 2.4 그 외

- 첫 진입 스플래시(로고·진행 바)와 화면 전체 실패용 오류 페이지 추가
- 시안 실측 대조: 앱 바 흰색 60px, 카드 격자·순위 칩·포커스 카드 색, 폴더형 탭, 상단 주황 카드 구성

---

## 3. 지금 실제로 도는 것

로컬 `pnpm --dir apps/web dev`에서 확인했다.

- 홈 순위 10건, 무한 휠, 스냅, 뒤로 가기 복원
- 테마 상세: 주황 카드(순위·관심 공백·수익률·상승 종목·데이터 반영), DAY-JA-VIEW 탭(기간별 중앙 반응·케이스 3건+아코디언·소재 Top3), 오늘 현황 탭(실시간/장 마감 후 근거·주도 종목)
- 유사사례 목록 / 과거 이벤트 상세 / 과거 소재 유형 상세
- 실시간 트리맵 10건
- 저장·저장 해제, 로그아웃
- 검증: lint / typecheck / 테스트 85건 / build / production fixture 경계 확인 모두 통과

**화면에 들어간 값의 출처**: 구 DAY-JA-VIEW DB(`theme.*`, `market.prices_daily`, `engine.event_outcomes`, `keyword.*`)에서 뽑아 `apps/web/src/adapters/legacyDemo.json`으로 만들었다. 만드는 스크립트는 `scripts/build_demo_from_legacy.py`. 지어낸 값은 없다.

---

## 4. 아직 없는 것

### 4.1 데이터가 없어 못 채우는 것

| 항목 | 막힌 이유 | 필요한 것 |
|---|---|---|
| **정본 테마 수익률** | PD-001의 상한형 유동시총 가중은 상장주식수·검증된 유동주식비율이 있어야 한다. 구 DB에도 없다 | 기준정보 수집 산출물 또는 KRX API 키 |
| **거래 관심 배수** | 같은 시각 과거 거래대금 기준선이 필요하고, 장중 누적 데이터가 있어야 한다. 코드는 있음(`SameTimeBaselinePolicy`, lookback 20·최소 5관측) | 장중 실시간 수집 |
| **급부상 배지** | 60초 순위 변동 자료 자체가 없다 | 장중 실시간 수집 |
| **소재 유형(온톨로지 라벨)** | E-17 라벨링 전이라 유형 대신 사건에 붙은 키워드를 쓰고 있다 | 온톨로지 검증 통과 |
| **자연어 요약** | 사건 문장을 그대로 쓰는 중 | 요약 생성 |

### 4.2 시연 데이터의 한계 (반드시 알고 볼 것)

- 수익률은 구 DB 종가 기준 **동일가중**이다. 정본(상한형 유동시총 가중)이 아니다. 계약상 `weightMethod` 값에 다른 선택지가 없어 `FREE_FLOAT_CAPPED`로 들어가 있으나, `legacyDemo.json`의 `weightMethod`는 `EQUAL_WEIGHT_LEGACY`로 표시해 두었다.
- 그래서 **테마 상세의 계산 기준 문구는 시연 데이터에는 맞지 않는다.** 실제 파이프라인 값이 들어오면 그대로 맞는다.
- T+1/T+5/T+20은 구 DB `engine.event_outcomes`에 이미 계산돼 있던 값을 그대로 쓴다.
- 기준일은 2026-08-10(전일 2026-08-07)이다.

### 4.3 배포

- `main`에 올려 뒀으나 Vercel 자동 배포가 걸려 있지 않다. 배포된 번들과 로컬 빌드 산출물이 다르다.
- 백엔드는 살아 있고 `/api/health`는 정상(postgresql HEALTHY, migrations APPLIED, fixtureMode false)이나 **DB가 비어 있어** 화면에 표시할 값이 없다.

### 4.4 아직 손대지 않은 것

- 로그인 게이트 완화는 **로컬 전용**이다. 운영 경로는 PD-006 그대로 두었다.
- 검색(리서치) 화면은 자리만 있고 내용이 없다.

---

## 5. 확인 방법

```bash
pnpm install
pnpm --dir apps/web dev        # 로컬 시연본 (구 DB 실데이터)
pnpm --dir apps/web dev:api    # 실제 API 연결
```

시연 데이터를 다시 만들려면(구 DB 접속 필요):

```bash
python scripts/build_demo_from_legacy.py --source "postgresql://<user>@<host>:<port>/dayjaview" --out apps/web/src/adapters/legacyDemo.json
```

검증:

```bash
pnpm --dir apps/web run lint && pnpm --dir apps/web run typecheck && pnpm --dir apps/web run test && pnpm --dir apps/web run build
```
